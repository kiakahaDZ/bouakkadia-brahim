import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kiakaha.portfolio.app',
  appName: 'fennecSoftPortfolio',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
